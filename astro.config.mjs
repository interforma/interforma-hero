import { defineConfig } from 'astro/config';

// Nota: @astrojs/sitemap se integra en el Bloque de SEO
// cuando existan todas las páginas de Release 0.

export default defineConfig({
  site: 'https://interforma.digital',
  output: 'static',
});
